import { presetSubGroupType } from "../../presetSubGroup.js";
import { glider } from "./glider.js";

export const general: presetSubGroupType = {
    name: "General",
    id: "general",
    items: [glider],
};
